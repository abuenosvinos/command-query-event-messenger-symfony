{% extends 'base.html.twig' %}

{% block title %}Users List{% endblock %}

{% block content %}

    <ul>
    {% for user in users.results %}
        <li><a href="{{ path('user-item', {"id":user.id.value}) }}">{{ user.email }}</li>
    {% endfor %}
    </ul>

{% endblock %}
